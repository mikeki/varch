<h1>Listing courses</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>CursoID</th>
    <th></th>
    <th></th>
  </tr>

<% @courses.each do |course| %>
  <tr>
    <td><%= link_to course.name, course %></td>
    <td><%= course.courseid %></td>
    <td><%= link_to 'Editar', edit_course_path(course) %></td>
    <td><%= link_to 'Eliminar', course, :confirm => 'Estás seguro?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= button_to('New Course', new_course_path, :method => :get) %>
